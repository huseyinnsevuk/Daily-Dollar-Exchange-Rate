name: Gunluk Kur Botu

on:
  schedule:
    - cron: '0 6 * * *' # Her sabah 09:00 (UTC 06:00)
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Repo'yu Çek
        uses: actions/checkout@v3

      - name: Python Kurulumu
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      # YENİ EKLENEN KISIM: Kütüphane Yükleme
      - name: Gereksinimleri Yükle
        run: |
          pip install -r requirements.txt

      - name: Scripti Çalıştır
        run: python main.py

      - name: Kaydet ve Gönder
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .
          # Eğer değişiklik yoksa hata vermemesi için || exit 0 ekliyoruz
          git commit -m "Otomatik Kur Güncellemesi 💰" || exit 0
          git push
